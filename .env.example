# Copy this file to .env.local and fill in the values.
# Only needed when running the frontend on a DIFFERENT machine from the backend.

# Set this to the backend server's IP address (not localhost).
# Example: VITE_BACKEND_URL=http://192.168.1.42:8000
VITE_BACKEND_URL=http://localhost:8000
